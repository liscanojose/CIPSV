</head>
	<body>
    	<!-- Mensaje -->
        	<div class="row">
                <% flash.each do |name, msg| -%>
                      <%= content_tag :div, msg, class: name%>
                <% end -%>
            </div>
        <!-- Mensaje -->

    <div class="container-alone">
        <div class="row col-lg-12">
            <div class="col-sm-offset-4 col-md-4 login loginmodal-container">
                <div style="padding:0px; margin:0px; display:none">
                    <% flash.each do |name, msg| -%>
                        <%= content_tag :div, msg, class: name %>
                    <% end -%>
                </div>

				<% provide(:title, "Clave Olvidada") %>
				<h1>Clave olvidada</h1>

				<div class="row">
				  <div class="col-md-6 col-md-offset-3">
				    <%= form_for(:password_reset, :url=> { :action => :create }) do |f| %>
				      <%= f.label :correo %>
				      <%= f.email_field :correo, class: 'form-control' %>

				      <%= f.submit "Submit", class: "btn btn-primary" %>
				    <% end %>
				  </div>
				</div>

            </div>
        </div>

    </div>